# ~/.config/chezmoi/chezmoi.toml として展開される (chezmoi init 時)
[git]
    # re-add / add のたびに自動コミットして toil を消す。
    # push は /dotfiles スキル経由でレビューしてから行うので autoPush はしない。
    autoCommit = true
    autoPush = false
